# FabricOS target configuration
# Uncomment for FabricOS builds (bare metal)
# [build]
# target = "x86_64-unknown-none"
# 
# [target.x86_64-unknown-none]
# linker = "rust-lld"
# rustflags = ["-C", "link-arg=-Tlink.x", "-C", "relocation-model=static", "-A", "warnings"]
# 
# [unstable]
# build-std = ["core", "compiler_builtins", "alloc"]
# build-std-features = ["compiler-builtins-mem"]

# Desktop build configuration (default)
[build]
target = "x86_64-pc-windows-msvc"
