[package]
name = "loom-layout"
version = "0.1.0"
edition = "2021"

[features]
default = ["std"]
std = ["loom-core/std"]

[dependencies]
loom-core = { path = "../loom-core", default-features = false }
